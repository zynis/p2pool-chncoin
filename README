WARNING: Only use this for chncoin (cnc) , any other mainstream coin see https://github.com/forrestv/p2pool

Changes for using this for chncoin
- Use --net litecoin
- ltc-scrypt module need to be install ( Same as litecoin used, and its included in the files )

Example on running p2pool
screen -d -m -S fcpool ~/p2pool-chncoin/run_p2pool.py --net chncoin -a YourCNCaddress -f 3 --give-author 0 --w 8400 --bitcoind-p2p-port 8106 --bitcoind-rpc-port 8108 rpcuser rpcpass

-f is fee percent
--w port you connect browser and miner to
